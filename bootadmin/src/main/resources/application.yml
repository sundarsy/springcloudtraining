eureka:
  client:
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 9100

spring:
  boot:
    admin:
      notify:
        slack:
          enabled: true
          #webhook-url: <GIVE_YOUR_WEBHOOK_HERE>